# Runtime dependencies, exact-pinned.
#
# These were previously floating (>=), which is how an unrelated Starlette
# release changed the TemplateResponse signature underneath us and turned the
# test suite red. A judge cloning this repo must get the versions we tested.
# Transitive dependencies are left to the resolver; only direct ones are here.

fastapi==0.140.13
uvicorn[standard]==0.51.0
pydantic==2.13.4
jinja2==3.1.6

# Engine: DNS resolution, HTTP probing, X.509 parsing, IDN and public-suffix
# handling. cryptography is required because ssl.getpeercert() returns nothing
# on an unverified socket, which is exactly the expired/self-signed case the
# TLS check exists to catch -- so we parse the DER ourselves.
dnspython==2.8.0
httpx==0.28.1
cryptography==49.0.0
idna==3.18
tldextract==5.3.1
